channels:
  - conda-forge
dependencies:
  # Build dependencies
  - hatch

  # Installation dependencies
  - pyyaml
  - requests
  - packaging
  - py-rattler

  # For autoformat with Black
  - pre-commit

  # For testing
  - pytest
  - pytest-cov
  - flaky
