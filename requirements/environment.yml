channels:
  - conda-forge
dependencies:
  # Build dependencies
  - hatch

  # Installation dependencies
  - pyyaml
  - requests

  # For autoformat with Black
  - pre-commit

  # For testing
  - pytest
  - pytest-cov
